
if [ "$#" -eq 1 ]; then
  INSTANCE_NAME=$1
  /etc/init.d/tomcat-{{env_app}}-{{project_cluster_name}}-${INSTANCE_NAME} profiling
else
  for INST in `ls /etc/init.d/tomcat-{{env_app}}-{{project_cluster_name}}-server*`
  do
    ${INST} profiling
  done
fi
